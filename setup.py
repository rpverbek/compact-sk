from setuptools import find_packages, setup

setup(
    name='compact',
    packages=find_packages(),
    version='0.0.1',
    description='COMPACT project code',
    author='ffng',
    install_requires=[]
)
